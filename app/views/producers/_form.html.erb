<%= form_for @producer do |f| %>
  
  <% if @producer.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@producer.errors.count, "error") %>
prohibited 
        this article from being saved:
      </h2>
      <ul>
        <% @producer.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <p>
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </p>

  <p>
    <%= f.label :text %><br>
    <%= f.text_area :text %>
  </p>  

  <p>
    <%= f.label :track %><br>
    <%= f.file_field :track %>
  <p>
    <%= f.submit %>
  </p>

  <% end %>    