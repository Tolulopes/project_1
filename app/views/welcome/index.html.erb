<!-- <h1>Underground</h1>
<p>welcome to underground the place for upcoming producers to showcase their music</p> -->

<h1>UnderGround</h1>
<p>underground the app blah blah</p>

<% if user_signed_in? %>
    <li>
      <%= image_tag(current_user.image) %>
      <%= link_to "#{current_user.try(:first_name)} Hubb", user_path(current_user) %>
      <% @user.tracks.each do |track| %>
        <%= track.name %>
        <%= audio_tag(track.song, controls: true ) %>
      <% end %>
    </li>
  <% else %>
    <li>
    <%= link_to('Login', new_user_session_path)  %>  
    </li>
<% end %>